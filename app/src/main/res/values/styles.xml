<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!-- Only API 21+ attributes here (minSdk 24); the real theming
         is handled by Compose in ui/theme/Theme.kt -->
    <style name="Theme.TrikRide" parent="android:Theme.Material.Light.NoActionBar">
        <item name="android:colorPrimary">@color/primary</item>
        <item name="android:colorAccent">@color/tertiary</item>
        <item name="android:windowBackground">@color/background_light</item>
        <item name="android:textColorPrimary">@color/text_primary</item>
        <item name="android:textColorSecondary">@color/text_secondary</item>
    </style>
</resources>
